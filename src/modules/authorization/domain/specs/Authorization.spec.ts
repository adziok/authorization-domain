import { Authorization } from '../Authorization';

describe('Authorization', () => {
    describe('-->  new()', () => {
        it.todo('--> confirm()')
        it.todo('--> setNewPassword()')
    })
});